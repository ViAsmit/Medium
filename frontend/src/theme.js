import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  typography: {
    fontFamily: [
      "sohne",
      '"Helvetica Neue"',
      "Helvetica",
      "Arial",
      "sans-serif",
    ],
  },
});

export default theme;
